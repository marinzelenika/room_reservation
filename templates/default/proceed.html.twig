{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    {% include 'navbar.html.twig' %}

    <!--  <img src="{{ asset('build/hotel1.jpg') }}" alt="ACME logo" class="top-image"> -->

    {% for message in app.flashes('warning') %}

        <div class="alert alert-warning">
            {{ message }}
        </div>
    {% endfor %}

    <h3>Odabrali ste sobu {{ room.title }}</h3>

    <div class="example-wrapper">
        {{ form_start(form) }}
        {{ form_widget(form.date1) }}
        {{ form_widget(form.date2) }}
        {{ form_label(form.email, 'Email: ') }}
        {{ form_widget(form.email) }}
        {{ form_label(form.telephone, 'Broj telefona:  ') }}
        {{ form_widget(form.telephone) }}
        {{ form_label(form.name, 'Ime i prezime:  ') }}
        {{ form_widget(form.name) }}
        {{ form_label(form.room, 'Odaberi još soba:  ') }}
        {{ form_widget(form.room, {'attr': {'class':'regular-checkbox'} }) }}
        {{ form_end(form) }}
    </div>
{% endblock %}

